-- ZK proofs generated with scale=100 are cryptographically invalid.
-- The scale was changed to 20000 to satisfy circuit divisibility constraints.
-- All existing proofs must be deleted and regenerated.
DELETE FROM dovada_fiscala WHERE is_zk = true;
